const initialState = {
    players: [],
    round: 0,
    dealerId: 0,
    numOfPlayers: 4,
    smallBlind: 2
};

const tableReducer = (state = initialState, action) => {
    switch (action.type) {
        // case '':

        //     break;

        // default:
        //     break;
    }

    return state;
}

export default tableReducer;

