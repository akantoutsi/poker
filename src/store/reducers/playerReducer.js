const initialState = {
    seq: null,
    cards: [],
    cash: 0,
    isActive: 0,
    pot: 0
};

const playerReducer = (state = initialState, action) => {
    switch (action.type) {
        // case '':

        //     break;

        // default:
        //     break;
    }

    return state;
}

export default playerReducer;

