const initialState = {
    // cards: [],
    // seq: null,
    // cash: 0,
    // isActive: 0,
    // pot: 0,
    // smallBlindId: null,
    // bigBlindId: null
};

const playerReducer = (state = initialState, action) => {
    switch (action.type) {
        // case '':

        //     break;

        // default:
        //     break;
    }

    return state;
}

export default playerReducer;

