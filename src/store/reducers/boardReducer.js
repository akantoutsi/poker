const initialState = {
    cards: []              
};

const boardReducer = (state = initialState, action) => {
    switch (action.type) {
        // case '':

        //     break;

        // default:
        //     break;
    }

    return state;
}

export default boardReducer;

